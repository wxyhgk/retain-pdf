# Prompt Files

This folder contains editable prompt text used by the pipeline.

- `translation_system.txt`
  System prompt for translation requests.
- `translation_task.txt`
  Task text embedded in the translation user payload.
- `classification_system.txt`
  System prompt for full-page classification in `precise` mode.

If you want to tune model behavior, edit these files instead of changing Python code.
