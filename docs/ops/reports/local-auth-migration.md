# 本地鉴权配置迁移（2026-09-06）

默认开发配置现位于 `backend/api/auth.local.json`。移除了对旧
`services/api/auth.local.json` 的自动回退；显式 `RUST_API_ROOT`、桌面用户目录、
配置文件优先于环境变量的规则不变。未修改密钥、并发设置、端口或数据路径。

## 迁移与验证

- 新配置与旧文件逐字节相同，所有者保持不变，权限限制为 `600`。
- 先保留回退代码验证第一次重启；随后备份旧配置，移除回退、编译并第二次重启。
- 两次重启的 API 与 AI 健康检查均成功；正确 key 可访问只读任务接口，错误和
  缺失 key 均被拒绝。最终 `/ready` 的 db、ai_service、jobsd 均为 healthy。
- 配置相关 Rust 测试 18 项通过；隔离进程覆盖文件优先、缺失/空 key 环境兜底、
  损坏文件及无可用 key 报错。另有一个仅供这些子进程调用的 ignored 测试。
- 开发启动工具测试 14 项通过；Rust 全工作区二进制构建通过。

没有执行新的翻译任务、调用付费模型、触发 CI 或推送远程。
本次没有重新运行整个 Rust 工作区测试。

## 保留与回退

旧配置和迁移前 API 二进制保留在本地受限权限备份中，未提交到 Git。
旧空 `services/api` 已移除；旧 `services/data` 与 `.env` 目录未清理，不能据此
将整个 `services` 当作可删除目录。现有数据位置不变。

需要回退时先停止开发服务，恢复备份配置与旧二进制，再用原运行模式和端口
启动并验证鉴权。不要覆盖后续更新的配置，也不要提交真实配置或备份。
