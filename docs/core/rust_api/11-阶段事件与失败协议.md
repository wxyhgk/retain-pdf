# 阶段事件与失败协议

这份文档定义 Rust API 和 Python worker 之间的阶段事件、进度事件和失败对象边界。

目标是让前端不再靠 `message`、`log_tail` 或 stdout 文本猜状态。

## 1. 三层事件来源

当前后端存在三层事件：

1. Python 原始 pipeline events
   - 文件：`DATA_ROOT/jobs/{job_id}/logs/pipeline_events.jsonl`
   - 写入方：Python OCR / translation / render worker
   - 用途：阶段内细粒度进度、artifact 发布、失败上下文

2. Rust DB events
   - 表：SQLite `events`
   - 写入方：Rust job runner
   - 用途：job 创建、状态变更、终态、兼容旧事件

3. Rust API canonical events
   - 接口：`GET /api/v1/jobs/{job_id}/events`
   - 组装方：`services/jobs/presentation/live_stage`
   - 用途：前端和第三方唯一应消费的公共事件流

前端只消费第三层。第一层和第二层是后端内部来源。

## 2. Python 原始事件契约

Python 写入 `pipeline_events.jsonl` 时，必须稳定带这些字段：

```json
{
  "job_id": "job-123",
  "seq": 12,
  "ts": "2026-05-28T10:12:33Z",
  "created_at": "2026-05-28T10:12:33Z",
  "level": "info",
  "user_stage": "translation",
  "stage": "translating",
  "substage": "translation_batches",
  "stage_detail": "已完成第 18/55 批翻译",
  "event_type": "stage_progress",
  "semantic_event_type": "progress",
  "message": "已完成第 18/55 批翻译",
  "progress_current": 18,
  "progress_total": 55,
  "progress_unit": "batch",
  "provider": "",
  "provider_stage": "",
  "elapsed_ms": 193822,
  "payload": {}
}
```

字段规则：

- `user_stage`
  Python 侧粗阶段，只允许：`ocr | translation | render | done`。
- `stage`
  Python 内部机器阶段，例如 `translating`、`render_preprocess`。
- `substage`
  机器可读子阶段，是前端状态语义的核心字段。
- `stage_detail`
  给用户看的短文案。
- `message`
  日志文案，只给人看；不要承载机器语义。
- `progress_current` / `progress_total` / `progress_unit`
  必须和当前 `user_stage + substage` 对齐。
- `semantic_event_type`
  Python 对旧 `stage_progress` / `artifact_published` 等事件的语义归一化，Rust 仍会二次 canonicalize。
- `payload`
  只能放扩展数据，不放主语义。

禁止出现这种事件：

```json
{
  "user_stage": "translation",
  "stage": "translating",
  "message": "render payload prewarm: ready"
}
```

正确写法：

```json
{
  "user_stage": "render",
  "stage": "render_preprocess",
  "substage": "render_prewarm",
  "message": "render payload prewarm: ready"
}
```

## 3. 稳定子阶段表

| user_stage | stage | substage | progress_unit | 说明 |
| --- | --- | --- | --- | --- |
| `ocr` | `ocr_processing` | `ocr_processing` | `page` | OCR 页进度。 |
| `ocr` | `normalizing` | `normalizing` | `step` | OCR 结果标准化。 |
| `translation` | `translation_prepare` | `translation_prepare` | `step` | 翻译准备。 |
| `translation` | `domain_inference` | `domain_inference` | `step` | 领域识别。 |
| `translation` | `page_policies` | `page_policies` | `page` | 页面策略和块分类。 |
| `translation` | `continuation_review` | `continuation_review` | `page` | 跨栏/跨页连续段复核。 |
| `translation` | `translating` | `translation_batches` | `batch` | 主翻译批次。 |
| `translation` | `translating` | `translation_tail_retry` | `batch` | 尾部重试队列。 |
| `translation` | `garbled_repair` | `garbled_repair` | `page` | 乱码修复。 |
| `translation` | `agent_repair` | `agent_repair` | `page` | 翻译结果修复。 |
| `translation` | `final_untranslated_recovery` | `final_untranslated_recovery` | `page` | 最终未翻译收口。 |
| `render` | `render_prepare` | `render_prepare` | `step` | 渲染准备。 |
| `render` | `render_preprocess` | `render_prewarm` | `step` | 渲染预热。 |
| `render` | `rendering` | `render_pages` | `page` | 按页渲染、背景清理、Typst source 构建。 |
| `render` | `rendering` | `render_compile` | `step` | Typst 编译和 PDF 保存。 |

新增子阶段时，必须同步更新：

- Python：`services/pipeline_shared/events.py`
- Rust：`models/job/stage.rs`
- Rust API 测试：`src/api_tests/jobs_query.rs`
- Python 协议测试：`devtools/tests/translation/test_pipeline_events_protocol.py`
- 本文档

## 4. Rust canonical event 契约

Rust API 对外返回的事件不是原始 Python 行，而是 canonical event。

对外字段示例：

```json
{
  "job_id": "job-123",
  "seq": 24,
  "ts": "2026-05-28T10:12:33Z",
  "created_at": "2026-05-28T10:12:33Z",
  "level": "info",
  "lane": "main",
  "stage": "translation",
  "substage": "translation_batches",
  "stage_detail": "已完成第 18/55 批翻译",
  "event": "stage_progress",
  "event_type": "progress",
  "raw_event_type": "stage_progress",
  "progress": {
    "unit": "batch",
    "current": 18,
    "total": 55,
    "percent": 32.72727272727273
  },
  "message": "已完成第 18/55 批翻译",
  "payload": {
    "raw_stage": "translating",
    "raw_user_stage": "translation",
    "raw_event_type": "stage_progress"
  }
}
```

公共 `stage` 只表达前端显示层：

- `ocr`
- `translation`
- `render`
- `done`

公共 `event_type` 只表达事件类型：

- `progress`
- `artifact`
- `terminal`
- `error`
- `diagnostic`

`user_stage` 是内部来源字段，API 输出会默认隐藏。前端不要依赖它。

## 5. lane 规则

`lane` 用来解决“后台事件覆盖主状态”的问题。

- `main`
  主进度。状态卡可以显示。
- `background`
  后台预热、缓存构建等。不能覆盖主阶段。
- `artifact`
  产物发布。不能覆盖主阶段。
- `diagnostic`
  诊断和错误辅助信息。

当前固定规则：

- `substage=render_prewarm` -> `lane=background`
- `event_type=artifact` -> `lane=artifact`
- `event_type=diagnostic` 或失败辅助事件 -> `lane=diagnostic`
- 其他进度事件 -> `lane=main`

## 6. stage snapshot 快照

`GET /api/v1/jobs/{job_id}` 和列表接口会返回权威的 `stage_snapshot`、`background_snapshots` 和 `stages`。

选择规则：

- 只从 `lane=main` 中选主阶段。
- `artifact` 和 `background` 事件不能覆盖主阶段。
- `render_prewarm` 这类翻译期间的渲染预处理会进入 `background_snapshots`。
- render 阶段优先展示 `render_pages` 的页级进度；`render_compile` 是 step 进度，不能伪装成“共 N 页”。
- 失败时如果终态事件没有进度，会保留最后可信进度。
- `status=succeeded|failed|canceled` 时 `stage_snapshot=null`，终态只由 `status` 表达。
- `display_stage=done` 永不出现。

详情接口中的 `stage_snapshot.progress` 是当前快照：

```json
{
  "current": 120,
  "total": 533,
  "percent": 22.5140712945591,
  "unit": "page"
}
```

前端刷新页面时只读详情接口的 `stage_snapshot` 判断当前阶段；事件流只用于历史、调试和时间线，不参与当前阶段选择。`stage_detail` 只做文案，`stage` 只做后端内部机器阶段。

## 7. 进度单调性

Python writer 会按 `(user_stage, substage, progress_unit)` 维护单调进度。

要求：

- 同一 key 下 `progress_current` 不倒退。
- 不同 substage 可以独立计数，例如 `render_pages` 和 `render_compile` 互不影响。
- `render_prewarm` 是后台 step，不参与主渲染页数。
- 新任务或重试任务必须重新写事件，不复用旧任务事件 seq。

## 8. 失败协议

正式失败对象是 job detail 中的 `failure`。

示例：

```json
{
  "failed_stage": "translation",
  "provider": "translation",
  "provider_stage": "continuation_review",
  "failure_code": "upstream_timeout",
  "failure_category": "timeout",
  "provider_code": null,
  "summary": "翻译阶段超时",
  "root_cause": "provider timed out",
  "retryable": true,
  "upstream_host": "api.deepseek.com",
  "suggestion": "从断点恢复任务",
  "last_log_line": "request timed out",
  "raw_excerpt": "timeout"
}
```

字段分层：

- `failed_stage`
  公共失败阶段，优先使用 `ocr | translation | render | done`。
- `provider` / `provider_stage`
  上游或内部子阶段归属。
- `failure_code`
  稳定机器码。
- `failure_category`
  粗粒度错误类别。
- `provider_code`
  上游 provider 原始错误码。
- `summary` / `root_cause` / `suggestion`
  面向前端和操作者的可读信息。
- `raw_excerpt`
  脱敏后的原始片段。

`failure_diagnostic` 是兼容投影，不是新代码的写入目标。

前端需要干净失败信息时，优先用：

`GET /api/v1/jobs/{job_id}/diagnostics`

该接口也会透出可选的 `render_diagnostics`。数据源是
`artifacts/pipeline_summary.json` 的同名字段，当前主要记录渲染阶段的兜底行为，
例如 `typst_cover_fallback_pages` 和 `typst_cover_fallback_items`。这些字段用于定位
“物理删除失败后改用 Typst 背景覆盖”的页和 block，不参与失败分类，也不改变
`failed_stage` / `retryable` / `resume_available` 的语义。

## 9. 验收标准

事件和失败协议改动必须满足：

- 前端不靠 `message` 判断阶段。
- render 相关事件不会带 `user_stage=translation`。
- `render_prewarm` 不覆盖主渲染进度。
- `/events` 输出只暴露公共 `stage` 和稳定 `substage`。
- `/jobs/{job_id}` 的 `progress` 是刷新后可用的最终快照。
- 失败信息优先来自 `failure`，不是 `log_tail` 或 stdout 文本。

## 10. 对应代码

Python 写入：

- `backend/pipeline/retainpdf_pipeline/services/pipeline_shared/events.py`
- `backend/pipeline/retainpdf_pipeline/translate/workflow/stages.py`
- `backend/pipeline/retainpdf_pipeline/translate/llm/domain_context.py`
- `backend/pipeline/retainpdf_pipeline/render/source/prewarm.py`
- `backend/pipeline/retainpdf_pipeline/render/source/prewarm_payload.py`
- `backend/pipeline/retainpdf_pipeline/ocr/ocr_provider/paddle_runner.py`

Rust 读取与 canonicalize：

- `backend/api/src/services/jobs/live_stage/pipeline_events.rs`
- `backend/api/src/services/jobs/live_stage/canonical_events.rs`
- `backend/api/src/services/jobs/live_stage.rs`
- `backend/packages/retain-core/src/models/job/stage.rs`

测试：

- `backend/pipeline/devtools/tests/translation/test_pipeline_events_protocol.py`
- `backend/api/src/api_tests/jobs_live_stage.rs`
