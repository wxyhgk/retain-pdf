#set page(width: 595pt, height: 841pt, margin: 0pt)
#set text(font: "Droid Sans Fallback", size: 11.5pt)
#place(top + left, dx: 85pt, dy: 84pt)[#box(width: 421pt)[$A _ { k c , k c } ^ { \prime }$ larger than the energy threshold. On the other hand, the configurations $_ { i a }$ have diagonal elements $A _ { i a , i a } ^ { \prime }$ smaller than the threshold and are therefore included in the matrix $\mathbf { A } ^ { \prime }$ from the very beginning.]]
