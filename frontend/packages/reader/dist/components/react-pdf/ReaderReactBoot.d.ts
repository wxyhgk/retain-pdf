import type { ReactElement } from "react";
export type ReaderReactBootProps = {
    loading: boolean;
    failed: boolean;
    text: string;
    percent: number;
};
export declare function ReaderReactBoot({ loading, failed, text, percent, }: ReaderReactBootProps): ReactElement | null;
//# sourceMappingURL=ReaderReactBoot.d.ts.map