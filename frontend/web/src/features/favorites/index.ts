// favorites —— 收藏页：列出收藏的书摘/页面并跳转阅读器。
//
// 这是本功能对外的唯一出口。阅读器内的收藏面板是 @retainpdf/reader 的实现，
// 不在本功能范围内。

export { FavoritesView } from "./ui/FavoritesView.jsx";
export type { FavoritesViewProps } from "./ui/FavoritesView.jsx";
