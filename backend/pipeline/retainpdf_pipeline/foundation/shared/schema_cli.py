SOURCE_JSON_MAINLINE_HELP = (
    "OCR JSON source path. Mainline prefers normalized document.v1.json; "
    "raw provider JSON is accepted only as an explicit adapter input and will be normalized first."
)

EXPLICIT_SOURCE_JSON_MAINLINE_HELP = (
    "Explicit OCR JSON source path. Mainline prefers normalized document.v1.json; "
    "raw provider JSON is accepted only as an explicit adapter input and will be normalized first."
)
