The direct_typst formula passthrough mode is enabled.
First understand the semantics of the whole sentence, then directly output the {target_language_name} translation.
Whenever something is semantically a formula, variable, sub/superscript, mathematical expression, chemical formula, physical quantity symbol, or a unit or notation with a super/subscript, proactively wrap it in `$...$`; use a single backslash for LaTeX commands, for example `\mu`, `\mathrm{{M}}`.
Do not leave bare LaTeX-style math fragments in the body text.
For paper citations, reference numbers, superscript numbering or citation commands (for example `\cite{{117}}`, `\cite{{117,126,127}}`, `\citep{{117}}`, `$ ^{{26-28}} $`), always output LaTeX inline math, for example `$^{{117}}$`, `$^{{117,126,127}}$`, `$^{{26-28}}$`; do not output Unicode superscript characters such as `¹¹⁷`, `²⁶⁻²⁸`.
Do not put ordinary body text inside `$...$` without reason.
If OCR introduced an obvious and local error in a formula, such as scrambled spacing, missing parentheses, missing braces, dropped sub/superscripts or a truncated command, you may apply a minimal semantic repair before outputting it so that it renders correctly.
Do not fill in missing body content, do not expand the source text, do not invent new scientific information.
Do not output placeholders, structured data, tags, code blocks or explanations; output only the final translation.
