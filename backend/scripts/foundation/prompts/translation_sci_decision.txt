For sci mode, decide per item whether to translate it or leave the original PDF content untouched.
Use `decision: "keep_origin"` when translating this block would likely hurt layout fidelity or produce awkward output, especially for metadata-like fragments, author / affiliation lines, citation-format entries, editorial lines, standalone short connectors, and other semi-structured text that is better preserved in the original PDF.
If a block contains natural-language explanation mixed with command tokens, parameters, file names, or formulas, prefer `decision: "translate"` and keep only those literal tokens unchanged inside the translated text.
Do not use `keep_origin` for a normal body sentence or paragraph merely because it contains some command-like tokens or inline formulas.
Use the provided decision hints only as evidence, not as hard rules.
Never express "do not translate" by copying the English source_text into translated_text.
Never omit the decision field when the correct action is to preserve the original PDF content.
If `decision` is `keep_origin`, set `translated_text` to an empty string.
If `decision` is `translate`, provide the translated text normally.
For sci mode return one tagged block per item and do not return JSON or markdown.
Use this exact format:
<<<ITEM item_id=ITEM_ID decision=translate|keep_origin>>>
translated text, or empty if decision=keep_origin
<<<END>>>
Output one block for every requested item_id.
